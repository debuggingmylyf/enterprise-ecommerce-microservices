server:
  port: 8888

spring:
  application:
    name: CONFIG-SERVER
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:./local-config

custom:
  message: Hello from local config!

#  cloud:
#    config:
#      server:
#        git:
#          uri: https://github.com/shubhamkochas/ecommerce-config-repo
#          clone-on-start: true


